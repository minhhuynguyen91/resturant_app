
<h1> Order for <%= @food_item.name %> </h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each_with_index do |order, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= link_to 'Show', food_item_order_path(@food_item, order) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
